{
  "name": "not-to-do-list",
  "version": "1.0.0",
  "description": "this is the back end of not to do lis",
  "main": "app.js",
  "type": "module",
  "scripts": {
    "dev": "nodemon backend-node-api/app.js",
    "start": "node backend-node-api/app.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm install --prefix react-frontend-app && npm run build --prefix react-frontend-app"


  },
  "author": "niraj bohora",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "helmet": "^4.6.0",
    "mongoose": "^6.0.2",
    "morgan": "^1.10.0"
  }
}
